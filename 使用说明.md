# 使用说明

> 这就是你的操作手册。忘了怎么弄的时候，打开这个文件看。

---

## 一、这些东西分别是干什么的

| 名字 | 干什么用 |
| --- | --- |
| **写新文章.bat** | 双击它，新建一篇文章 |
| **生成网站.bat** | 双击它，把文章变成网页 |
| **预览网站.bat** | 双击它，在自己电脑上先看看效果 |
| **发布到网上.bat** | 双击它，上传到网上让所有人能看到 |
| **检查域名.bat** | 双击它，检查域名和解析生效了没有 |
| `posts` 文件夹 | **你的文章都在这里**，一篇文章一个文件 |
| `设置.txt` | 改博客名字、副标题、页脚 |
| `about.md` | 「关于」页面的内容 |
| `images` 文件夹 | 文章里要用的图片放这里 |
| `theme` 文件夹 | 网站的样式（颜色、字号） |
| `docs` 文件夹 | 自动生成的网站，**不要手动改里面的东西** |
| 其余 .js 文件 | 程序文件，不用管它们 |

---

## 二、平时写文章（就三步）

1. 双击 **写新文章.bat**，按提示填三个问题（标题 / 标签 / 摘要）。
2. 记事本会自动打开一篇新文章，写完按 `Ctrl+S` 保存，然后关掉记事本。
3. 双击 **生成网站.bat**，再双击 **发布到网上.bat**。

等一两分钟，刷新网站就能看到新文章了。

> 想先在电脑上看看效果，中间那一步可以改成双击 **预览网站.bat**。

---

## 三、第一次发布（只做一次，大约 10 分钟）

### 第 1 步：在 GitHub 建一个空仓库

1. 打开 <https://github.com/new?name=xiaoruifan-blog&visibility=public>
   （这个链接会**自动帮你填好仓库名字**，比手动打省事）
2. 确认 **Repository name** 是 `xiaoruifan-blog`
3. 确认选的是 **Public**（必须选公开，免费账号只有公开仓库能用 Pages）
4. 下面那些「Add a README file」之类的勾选框**一个都不要勾**
5. 点绿色的 **Create repository**

> 注意：**不要**用你原来那个 `blog` 仓库。那个里面装着你的双拼练习站，是另做一套，两者互不影响。

### 第 2 步：把博客传上去

双击 **发布到网上.bat**。

- 第一次会**弹出一个浏览器窗口**让你登录 GitHub（因为你之前的登录信息已经过期了）。
  用 `shimangSB` 这个账号登录，点 **Authorize / 授权** 就行，以后就不用再登了。
- 看到「**发布成功**」四个字就完成了。

> 如果提示失败，先确认第 1 步的仓库名字确实叫 `xiaoruifan-blog`，而且是在浏览器里真的建好了。

### 第 3 步：打开 GitHub Pages 开关

1. 打开 <https://github.com/shimangSB/xiaoruifan-blog/settings/pages>
2. **Source** 选 `Deploy from a branch`
3. **Branch** 选 `main`，右边的文件夹选 `/docs`
4. 点 **Save**
5. 等 1 分钟，页面上方会出现一个绿色提示，告诉你网站已经发布了

### 第 4 步：填上你的域名

还在刚才那个 Pages 设置页面：

- **Custom domain** 里填 `xiaoruifan.xyz`，点 **Save**

（因为生成网站时已经自动生成了 CNAME 文件，这一步有可能已经帮你填好了，没有的话就手动填一次。）

### 第 5 步：回阿里云加解析记录

#### 先用 30 秒搞懂「解析」是什么

- 你的域名 `xiaoruifan.xyz` 是一个**门牌号**，好记，但它自己不指向任何地方。
- 你的博客真正住在 GitHub 的服务器上，那台机器的**真实地址是一串数字**：`185.199.108.153`。
- **加解析 = 在「门牌号登记簿」上写一行字**：以后谁找 `xiaoruifan.xyz`，就告诉他去 `185.199.108.153`。
- 这本登记簿由阿里云替你保管，所以你只要在阿里云的控制台里写这几行字。

就这么简单。你要写 **5 行**。

#### 怎么点（照着做）

1. 浏览器打开 <https://dns.console.aliyun.com>，用买域名的那个账号登录。
2. 页面上会列出 `xiaoruifan.xyz`，点它右边的「**解析设置**」。
3. 进去后能看到一个记录列表，右上角有「**添加记录**」按钮，点它，会弹出一个小表单。
4. 表单里只有 4 个格子要管，照着下面填：

**第 1 条**

| 格子 | 填什么 |
| --- | --- |
| 记录类型 | 下拉选 `A` |
| 主机记录 | `@` |
| 解析线路 | 默认（不用动） |
| 记录值 | `185.199.108.153` |
| TTL | 10 分钟（不用动） |

填完点「**确认**」。

**第 2、3、4 条**：还是点「添加记录」，类型还是 `A`，主机记录还是 `@`，**只有「记录值」不一样**：

| 第几条 | 记录值 |
| --- | --- |
| 第 2 条 | `185.199.109.153` |
| 第 3 条 | `185.199.110.153` |
| 第 4 条 | `185.199.111.153` |

**第 5 条**（这条和上面不一样，注意看）：

| 格子 | 填什么 |
| --- | --- |
| 记录类型 | 下拉选 `CNAME` |
| 主机记录 | `www` |
| 解析线路 | 默认 |
| 记录值 | `shimangSB.github.io` |
| TTL | 10 分钟 |

点「确认」。一共 5 条，收工。

#### 最容易搞错的 5 个地方

1. **`@` 就是一个 @ 符号**，代表「域名本身」。别写成 `www`，也别留空。
2. **第 5 条的记录值只写 `shimangSB.github.io`** —— 不要加 `https://`，不要加斜杠，末尾不要加点。
3. **如果提示「记录冲突」**：说明已经有同名记录了，把旧的那条删掉再加新的。
4. **阿里云有时自带一条 `@` 记录**（指向某个广告停放页）。如果它的记录值不是上面那 4 个 IP 之一，**把它删掉**，否则你的域名会打开一个莫名其妙的页面。
5. `www` 不要写成 `wwww`，也别写成 `www.`。

#### 重要：你的域名刚注册，要等

我查过了，**`xiaoruifan.xyz` 是今天才注册的**。阿里云这边已经托管好了（DNS 服务器是 `dns25/dns26.hichina.com`），但 `.xyz` 的注册局还需要时间把域名发布到全世界——在那之前**全世界都查不到这个域名**。

- 一般等 **几小时，最长 24 小时**。
- 这期间**你什么都不用做**，更不是操作错了。
- 随时双击 **检查域名.bat** 看进度。等它显示「🎉 全部就绪」，网站就能打开了。

### 第 6 步：打开 HTTPS（让网址变成 https 开头）

DNS 生效后再等一会儿（快的十几分钟，慢的 24 小时），回到第 3 步那个 Pages 设置页面：

- 勾选 **Enforce HTTPS**

勾得上就说明好了。勾不上就再等等，证书是 GitHub 自动申请的，需要时间。

### 最后，别忘了域名实名认证

阿里云买的域名**必须实名认证**，否则解析会被暂停，白忙一场。

- 阿里云控制台 → 域名 → 域名列表 → 点你的域名 → 实名认证 → 传身份证
- 审核一般几小时到 3 个工作日

---

## 四、写文章能用的格式（速查）

不用记，需要的时候回来查。完整的例子看 `posts` 文件夹里的「写作格式示例」。

| 想实现 | 怎么写 |
| --- | --- |
| 小标题 | `## 标题内容` |
| **加粗** | `**要加粗的字**` |
| *斜体* | `*要斜体的字*` |
| 列表 | 行首写 `- ` 加空格 |
| 引用块 | 行首写 `> ` 加空格 |
| 链接 | `[显示的文字](网址)` |
| 图片 | `![说明](images/图片名.jpg)` |
| 分割线 | 单独一行写 `---` |
| 分段 | **空一行**就是新的一段 |

**只需要记住：空一行分段，行首加 `##` 是标题。** 其他都无所谓。

---

## 五、常见问题

**问：打开 https://xiaoruifan.xyz 打不开，或者显示「找不到服务器」**
答：先双击 **检查域名.bat**，它会直接告诉你是哪一步没到位（域名没生效 / 解析没加 / 记录填错）。刚注册的域名要等几小时到 24 小时，这是最常见的正常情况。

**问：打开网站是 404，或者显示「There isn't a GitHub Pages site here」**
答：多半是第 3 步的分支或文件夹选错了。确认是 `main` + `/docs`。改完等 1 分钟。

**问：网上的内容和我在电脑上看到的不一样，或者没更新**
答：GitHub 部署要一两分钟。等一会儿按 `Ctrl+F5` 强制刷新（手机上是清一下浏览器缓存）。

**问：图片显示不出来**
答：图片要放进 `images` 文件夹（不是 `docs/images`），文章里写 `![说明](images/文件名.jpg)`，图片名不要有空格。然后重新生成 + 发布。

**问：发布的时候要求登录，或者提示 Authentication failed / Invalid username or token**
答：在弹出来的窗口里用 `shimangSB` 这个账号登录一次，点 Authorize。如果窗口没弹出来，按 `Win` 键搜「凭据管理器」→ **Windows 凭据** → 找到带 `github` 字样的那几条，全部删除，再双击一次发布。

**问：我把文章删了，网站上还在**
答：删掉 `posts` 里对应的 `.md` 文件，然后双击生成网站 + 发布。`docs` 文件夹每次都会重新生成，旧的会自动消失。

**问：网站上的地址太长了 / 想改文章的网址**
答：网址是根据文件名来的。想改的话，把 `posts` 里的文件名改掉（日期开头 + 名字），重新生成 + 发布。

**问：我不小心改了 `docs` 文件夹里的东西**
答：没关系，双击一次 **生成网站.bat**，它会全部重新生成，你的手改就没了——所以别改它。

**问：会影响我原来那个 GitHub Pages 网站吗？**
答：不会。你原来那个站（双拼练习 + 博客）在 `blog` 仓库里，网址是 <https://shimangSB.github.io/blog/>。这个新博客在 `xiaoruifan-blog` 仓库里，是完全独立的一套，改了这边不会影响到那边。

**问：为什么不在原来那个仓库里写博客？**
答：因为一个域名只能绑一个网站。绑到旧仓库的话，别人打开你的域名看到的会是双拼练习工具，而不是这个博客。分成两个仓库，两边都干净。

---

## 六、进阶：改样式

- 想换**主题色**：打开 `theme/style.css`，找到 `--accent` 那一行，把色号改掉（比如 `#e0523f` 是红色），重新生成即可。
- 想改**字号**：同一个文件里 `body` 的 `font-size`。
- 想改**博客名字 / 副标题 / 页脚**：编辑 `设置.txt`。

改完都要双击一次 **生成网站.bat**（要发布的话再加一次 **发布到网上.bat**）。

---

## 七、网址备查

- 你的博客：<https://xiaoruifan.xyz>
- 订阅地址（可以发给朋友）：<https://xiaoruifan.xyz/feed.xml>
- 仓库设置页：<https://github.com/shimangSB/xiaoruifan-blog/settings/pages>
- 你原来那个站：<https://shimangSB.github.io/blog/>
